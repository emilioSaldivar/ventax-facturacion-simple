# TASKS Control De Reintentos De Emision v0.1

## Alineacion

- `AGENTS.md`
- `docs/SPEC_CONTROL_REINTENTOS_EMISION_v0.1.md`
- `docs/PLAN_CONTROL_REINTENTOS_EMISION_v0.1.md`

## Estado General

- `SPEC`: DONE
- `PLAN`: DONE
- `TASKS`: Iniciativa CERRADA. CRE-002 a CRE-011 DONE.

## Matriz

| ID | Fase | Tarea | Estado | Criterio de aceptacion |
| --- | --- | --- | --- | --- |
| CRE-001 | SDD | Crear cadena SPEC/PLAN/TASKS de esta iniciativa | DONE | Existen los tres documentos versionados y referenciados entre si |
| CRE-002 | Tipos | Agregar `attempts`, `documentoCreatedAt`, `accionNotificadaAt` a `PendingFiscalEmission` y `outboxEstado` a la firma de `failPendingEmission` en `FacturaRepository` (`facturas.types.ts`) | DONE | Typecheck del workspace `api` compila sin errores tras el cambio de tipos |
| CRE-003 | Backend | `claimNextPendingEmission()`: agregar `o.attempts`, `f.created_at`, `f.accion_notificada_at` al `select`/`returning` y al mapeo de retorno (`facturas.repository.ts`) | DONE | Verificado contra Postgres real: el resultado incluye los tres campos con valores correctos (attempts post-incremento, created_at del documento, accion_notificada_at null en un documento nuevo) |
| CRE-004 | Backend | `failPendingEmission()`: reemplazar `'FAILED_TEMP'` hardcodeado por parametro `outboxEstado` (`facturas.repository.ts`) | DONE | Verificado contra Postgres real: invocar con `outboxEstado='FAILED_PERM'` deja la fila en ese estado y `claimNextPendingEmission()` deja de reclamarla en una corrida posterior |
| CRE-005 | Backend | Bajar `HORAS_PERSISTENCIA_ERROR_TEMPORAL` de `24` a `1` en `facturas.accion.ts` + actualizar comentario de cabecera | DONE | Test unitario existente/nuevo de `deriveAccion` para `ERROR_TEMPORAL` confirma `REQUIERE_SOPORTE` a partir de 1h de antiguedad, `EN_PROCESO` antes |
| CRE-006 | Backend | Agregar constantes `EMISION_BACKOFF_SECONDS`, `HORAS_CORTE_REINTENTO_EMISION` y helper `nextEmisionBackoffSeconds()` en `facturas.service.ts` | DONE | Test unitario de la tabla completa de backoff (attempts 1 a 9, confirma que 9 repite el valor de 8) |
| CRE-007 | Backend | Reescribir el bloque `catch` de `processNextQueuedFiscalEmission()`: calcular `cutover` por antiguedad, `outboxEstado`/`estado`/`recoverable` segun RN-2/RN-3, invocar `deriveAccion`+`notifyAccionRequerida`+`markAccionNotificada` segun RN-4/RN-5/RN-6 | DONE | Tests unitarios (CRE-009) en verde; revision de que `logger` y los imports nuevos (`deriveAccion`, `notifyAccionRequerida`) no generan ciclo de imports (`npm run typecheck` limpio) |
| CRE-008 | QA | Actualizar fakes/mocks de `FacturaRepository` en `apps/api/tests/facturas.service.test.ts` y `apps/api/tests/entrega.service.test.ts` para cumplir la interfaz nueva (`claimNextPendingEmission` devuelve los 3 campos nuevos, `failPendingEmission` acepta `outboxEstado`) | DONE | `npm run typecheck --workspace @facturacion-simple/api` sin errores; suite existente sigue en verde (sin regresiones respecto a los 6 fallos preexistentes ya documentados en memoria de proyecto, si siguen presentes) |
| CRE-009 | QA | Tests unitarios nuevos: backoff por `attempts`, corte a 24h (incluyendo caso `TIMEOUT` forzado a `ERROR_TEMPORAL`), disparo de notificacion guardado por `accionNotificadaAt`, notificacion que falla no revierte el fallo de emision | DONE | Casos descritos en PLAN, seccion "Estrategia De Testing", todos en verde en `apps/api/tests/facturas.service.test.ts` |
| CRE-010 | QA real | Validacion manual contra Postgres real (no solo fakes): forzar un fallo de emision repetido (mock/stub del gateway o error real controlado), confirmar backoff creciente en `next_attempt_at`, simular antiguedad >=24h (o reducir temporalmente el umbral para la prueba) y confirmar transicion a `FAILED_PERM`, confirmar que `retryDocumentoEmission` recupera el documento desde `FAILED_PERM` | DONE | Evidencia de la secuencia completa registrada en este documento (queries `psql` o logs), siguiendo el precedente de rigor de `TASKS_PERSISTENCIA_TIPO_SERVICIO_FACTURA_v0.1.md` (TSF-012) |
| CRE-011 | Cierre | Repasar `SPEC`/`PLAN`/`TASKS` contra la implementacion real y actualizar estados; registrar si `HORAS_PERSISTENCIA_ERROR_TEMPORAL=1` tuvo efectos secundarios inesperados en el badge de otros documentos `ERROR_TEMPORAL` no relacionados al outbox (ver Riesgos en PLAN) | DONE | Este documento actualizado; SPEC/PLAN sin desviaciones sin documentar |

## Evidencia

- 2026-08-24/25: `CRE-002` a `CRE-009` implementados localmente:
  - `facturas.types.ts`: `PendingFiscalEmission` con `attempts`/`documentoCreatedAt`/`accionNotificadaAt`; `failPendingEmission` con `outboxEstado: "FAILED_TEMP" | "FAILED_PERM"`.
  - `facturas.repository.ts`: `claimNextPendingEmission()` devuelve los 3 campos nuevos (via `o.attempts`, `f.created_at`, `f.accion_notificada_at` en el `RETURNING`); `failPendingEmission()` usa `outboxEstado` en vez de `'FAILED_TEMP'` hardcodeado.
  - `facturas.accion.ts`: `HORAS_PERSISTENCIA_ERROR_TEMPORAL = 1`.
  - `facturas.service.ts`: `EMISION_BACKOFF_SECONDS` (60/300/900/1800/3600/7200/14400/21600s), `HORAS_CORTE_REINTENTO_EMISION = 24`, `nextEmisionBackoffSeconds()`; `processNextQueuedFiscalEmission()` calcula `cutover` por antiguedad, fuerza `ERROR_TEMPORAL`+`recoverable:false`+`FAILED_PERM` al cortar, y dispara `deriveAccion`+`notifyAccionRequerida`+`markAccionNotificada` guardado por `accionNotificadaAt`.
  - `apps/api/tests/facturas.service.test.ts`: `FakeFacturaRepository` extendido con `claimNextVerificacion`/`scheduleNextVerificacion`/`markAccionNotificada`/`getNotificationRecipients` (+ `markAccionNotificadaCalls`, `notificationRecipients`, `failPendingEmissionCreatedAt` para pruebas deterministicas) y los 3 `pendingEmission` existentes completados con los campos nuevos. 8 tests nuevos: tabla de backoff (`it.each`, attempts 1 a 9), corte a 24h (caso generico y caso `TIMEOUT` forzado a `ERROR_TEMPORAL`), notificacion disparada/no disparada segun `accionNotificadaAt`, notificacion que falla no bloquea el fallo de emision.
  - `apps/api/tests/entrega.service.test.ts`: sin cambios necesarios (su fake de `claimNextPendingEmission` siempre devuelve `null`, nunca toca los campos nuevos).
  - Validado: `npm run typecheck --workspace @facturacion-simple/api` limpio; `npm run lint --workspace @facturacion-simple/api` limpio (alias de typecheck en este workspace); `npm run test --workspace @facturacion-simple/api`: 212/218 en verde. Los 6 fallos restantes se confirmaron **preexistentes e identicos** haciendo `git stash` de todos los cambios de esta iniciativa y corriendo la suite contra el commit base (`71c6f41`): mismos 6 nombres de test fallando (`entrega.service.test.ts` x2, `facturas.service.test.ts` x4), sin relacion con esta iniciativa — coincide con lo ya documentado en memoria de proyecto. Cero regresiones.
- 2026-08-24/25: `CRE-010` completado contra Postgres real local (`nuevo_repo-postgres-1`, puerto 5433, 30 migraciones ya aplicadas). Se insertaron manualmente una `facturas_operativas` (estado `ERROR_TEMPORAL`, `created_at` = hace 25h) y su `factura_emision_outbox` (estado `FAILED_TEMP`, `attempts=5`) sintéticas, reusando un facturador/tenant/usuario ya existente en la base de datos local, y se corrió `PgFacturaRepository`/`processNextQueuedFiscalEmission` real (script standalone via `tsx`, sin mocks):
  - `claimNextPendingEmission()` devolvió `attempts: 6` (incrementado), `documentoCreatedAt` = el `created_at` real de la fila (hace 25h), `accionNotificadaAt: null` — confirma CRE-003.
  - Con un gateway que lanza `FiscalGatewayError("UPSTREAM_ERROR", ...)`, `processNextQueuedFiscalEmission()` detectó `cutover=true` (25h >= 24h) y dejó el outbox en `estado: "FAILED_PERM"`, `last_error.recoverable: false`, `last_error.retry_after_seconds: null`, `last_error.suggested_action: "CONTACTAR_SOPORTE"`, y `facturas_operativas.estado: "ERROR_TEMPORAL"` — confirma CRE-004 y RN-2/RN-3.
  - Se disparó una notificación real (log `"[DEV] Email no enviado — RESEND_API_KEY y SMTP_HOST no configurados"` con destinatario resuelto de verdad desde `getNotificationRecipients`) — confirma el wiring real de `deriveAccion`+`notifyAccionRequerida` (RN-4), sin necesidad de mockear nada.
  - Una segunda llamada a `claimNextPendingEmission()` devolvió `null` — confirma que `FAILED_PERM` efectivamente deja de ser reclamado (el corte funciona).
  - `retryPendingEmission()` sobre el documento en `FAILED_PERM` lo recuperó: outbox pasó a `estado: "PENDING"`, `facturas_operativas.estado: "EMITIENDO"`, y una siguiente `claimNextPendingEmission()` volvió a tomar la misma fila — confirma que el camino de recuperación manual (ya existente, sin cambios de código) sigue funcionando sobre el nuevo estado `FAILED_PERM`.
  - Datos sintéticos eliminados al terminar (`DELETE` de ambas filas) — la base local quedó como estaba antes de la prueba.
- 2026-08-24/25: `CRE-011` (cierre). Efecto secundario confirmado y aceptado (ya anticipado como Riesgo en el PLAN): `HORAS_PERSISTENCIA_ERROR_TEMPORAL=1` también afecta el badge de `emitFacturaAgainstFiscalGateway()` (`facturas.service.ts:930-974`, el camino de emisión **síncrona**, distinto del outbox — no crea fila en `factura_emision_outbox` y por lo tanto nunca pasa por el backoff/notificación nuevos). Un documento que falla por esa vía ahora muestra `REQUIERE_SOPORTE` en la UI a la hora (antes: 24h), pero **no** dispara la notificación proactiva por correo (esa sigue exclusiva del worker de outbox). Se acepta como fuera de alcance de esta iniciativa (el SPEC solo cubre el outbox de emisión pre-emisión) — el badge más temprano ya es una mejora respecto al comportamiento anterior (24h), y no hay regresión: antes tampoco se notificaba proactivamente por esa vía. Sin cambios de código adicionales requeridos. `SPEC`/`PLAN` no requieren ajustes — el riesgo ya estaba documentado con precisión.
