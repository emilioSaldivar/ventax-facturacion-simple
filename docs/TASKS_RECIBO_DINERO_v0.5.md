# TASKS Recibo de Dinero v0.5

## Alineacion

- `docs/SPEC_RECIBO_DINERO_v0.5.md`
- `docs/PLAN_RECIBO_DINERO_v0.5.md`
- `spec/openapi.yaml`

## Descripcion del modulo

Migra la gestion/creacion de `recibos_dinero` de una implementacion 100% local (sin firma, sin validez legal) a un modelo que delega en `facturacion-electronica` (Recibos de Dinero Firmados Digitalmente, Ley N.º 6822/2021), siguiendo el mismo patron de integracion servidor-a-servidor que ya usa `facturas`. Alcance de esta version: **solo backend** (gestion/creacion/emision/anulacion). La UI de `apps/web-operacion` queda fuera de alcance.

## Bloqueante externo (no controlable desde este repo)

`/v1/recibos/*` **no esta desplegado** en `fe-test` ni `fe-prod` (verificado 2026-07-24: `GET /v1/recibos` en fe-test responde `404 Cannot GET`, no `401`). Todo lo que dependa de una llamada real al backend fiscal queda `BLOCKED` hasta que:
1. El equipo de `facturacion-electronica` despliegue esa superficie en `fe-test` (y luego en el entorno de `fe-prod`).
2. La `fe_consumer_api_key` de cada facturador tenga habilitados `RECIBO_WRITE`/`RECIBO_READ`/`RECIBO_VOID`.

## Matriz

| ID | Fase | Tarea | Estado | Criterio de aceptacion |
|---|---|---|---|---|
| RD5-001 | SDD | Redactar SPEC/PLAN/TASKS v0.5 | DONE | Cadena documental creada, referencia el bloqueo verificado y el patron `fiscal-gateway` a reusar. |
| RD5-002 | Docs | Vendorizar `facturacion-electronica-consumer-docs` actualizado | DONE | `docs/API_FACTURACION_ELECTRONICA/facturacion-electronica-consumer-docs/` incluye `GUIA_INTEGRACION_CONSUMIDORES_v0.2.md` (seccion 16), `openapi.consumidor-v0.3.yaml`, `README.md` actualizado con nota de vendoring; los archivos previos (`openapi.yaml` admin-inclusive, `OPERACION_*.md`) se conservan sin tocar por estar referenciados desde SPEC/PLAN/TASKS propios. |
| RD5-003 | DB | Migracion `0025_recibos_dinero_v05_fiscal.sql` | DONE (parcial) | Archivo creado: `ALTER TYPE recibo_estado ADD VALUE 'ANULADO'`; columnas nuevas agregadas sin dropear columnas viejas; indices unicos de idempotencia/external_ref creados. Validado por revision de SQL y contra la restriccion de Postgres de `ALTER TYPE ADD VALUE` (no se usa el valor nuevo en la misma transaccion/migracion). **No se ejecuto `npm run migrate` contra una DB real en esta sesion** (sin stack Postgres local disponible en este entorno) — pendiente de correr en el primer deploy a staging junto con RD5-016. |
| RD5-004 | fiscal-gateway — tipos | Agregar tipos `Fiscal*Recibo*` | DONE | `fiscal-gateway.types.ts` incluye `FiscalCrearReciboRequest`, `FiscalReciboResult`, `FiscalEditarReciboRequest`, `FiscalAnularReciboRequest`, `FiscalReciboVerificacionResult`; `FiscalGateway` declara los 9 metodos nuevos. `emisor_id`/`emisorId` se omitio en los metodos por-id (editar/eliminar/emitir/anular/pdf/xml) porque la guia de integracion no lo requiere ahi (solo en crear y en el listado). |
| RD5-005 | fiscal-gateway — mock | Implementar metodos de recibo en `MockFiscalGateway` | DONE | Fixtures deterministicos con estado en memoria (`Map`) cubren ciclo `BORRADOR→EMITIDO→ANULADO`, errores `RECIBO_NOT_FOUND`/`RECIBO_NOT_EDITABLE`/`RECIBO_NOT_DELETABLE`/`RECIBO_NOT_EMITTABLE`/`RECIBO_NOT_ANULABLE`/`RECIBO_ALREADY_ANULADO` reproducibles para tests, con la misma forma de error (`FiscalGatewayError` + `details.body.error`) que usaria `RealFiscalGateway`. |
| RD5-006 | fiscal-gateway — real | Implementar metodos de recibo en `RealFiscalGateway` | DONE (no probado contra fe-test) | Requests reales a `POST/PATCH/DELETE /recibos`, `POST /recibos/:id/emitir`, `POST /recibos/:id/anular`, `GET /recibos/:id/pdf`, `GET /recibos/:id/xml`, `GET /verificar/recibo/:token(/pdf)` (estos dos ultimos sin `x-api-key`, por ser publicos). Mapeo de errores de red/timeout reusa `fetchWithTimeout`/`mapFetchError` existentes. **No pudo probarse contra fe-test real** (bloqueado, ver seccion superior) — validado solo por typecheck/build y revision manual del contrato contra la guia. |
| RD5-007 | Backend — tipos | Reescribir `recibos.types.ts` | DONE | `ReciboEstado` incluye `ANULADO`; `ReciboRecord`/`ReciboCreateInput` reflejan el nuevo shape de SPEC seccion 5; `ReciboAnularInput` agregado; `RecibosRepository` expone `upsertFromFiscal()`/`markDeleted()` en vez de `create/update/emitir` propios. |
| RD5-008 | Backend — repository | Reescribir `recibos.repository.ts` como cache/mirror | DONE | `upsertFromFiscal` hace `INSERT ... ON CONFLICT (id) DO UPDATE` usando el `id` que devuelve facturacion-electronica; campos de solo-creacion (`external_ref`, `idempotency_key`, `referencia_documento_uuid`, etc.) usan `COALESCE` para no perderse en operaciones posteriores (editar/emitir/anular) que no los reenvian. `factura_numero_display` se lee como fallback de solo lectura para recibos historicos v0.1-v0.4. Sin referencias a `recibos_dinero_numeracion`. |
| RD5-009 | Backend — service | Reescribir `recibos.service.ts` | DONE | `crearRecibo/editarRecibo/eliminarRecibo/emitirRecibo/anularRecibo` delegan al `FiscalGateway` con idempotencia (`buildReciboExternalRef` + `findByIdempotencyKey`, solo en creacion) y `mapFiscalGatewayError` segun tabla de SPEC seccion 8. `verificarRecibo` combina estado fiscal + datos comerciales de cache (SPEC seccion 7), fail-closed si el backend fiscal dice invalido. |
| RD5-010 | Backend — routes | Actualizar `recibos.routes.ts` | DONE | Header `Idempotency-Key` opcional soportado en `POST /recibos` (a diferencia de `facturas`, no es obligatorio — el frontend actual no lo envia y no se quiso romper ese flujo en una iteracion backend-only); nuevos `GET /recibos/:id/xml` y `POST /recibos/:id/anular` (body `{motivo}`, Zod, max 500); `GET /recibos/:id/pdf` hace passthrough binario del gateway. |
| RD5-011 | Backend — limpieza | Eliminar `recibos.pdf.ts` | DONE | Archivo eliminado. `npm run typecheck`/`build` confirman que no quedo ningun import roto. |
| RD5-012 | Backend — verificacion | Actualizar `verificacion.routes.ts` para recibos | DONE | `GET /verificar/recibo/:token` usa `verificarRecibo` (hibrido); `GET /verificar/recibo/:token/pdf` hace passthrough a `fiscalGateway.verificarReciboPdf`. La verificacion de `nota` (presupuestos) no se toco. |
| RD5-013 | API — RD-010 | Actualizar creacion de recibo desde factura a credito | DONE | `POST /facturas/:documentoId/recibo` pasa `referencia_documento_uuid: factura.document_uuid` y `referencia_documento_numero_display: factura.numero_fiscal` en vez de los campos viejos. |
| RD5-014 | API — OpenAPI | Actualizar `spec/openapi.yaml` (propio) | DONE | Campos nuevos en `ReciboRecord`/`ReciboCreateRequest`/`ReciboUpdateRequest`/`ReciboVerificacionResponse`, `ANULADO` agregado a los enums de estado, paths `GET /recibos/{id}/xml` y `POST /recibos/{id}/anular` documentados, `factura_id`/`factura_numero_display` marcados `deprecated: true`. Validado parseable con `js-yaml` (88 schemas, 54 paths). |
| RD5-015 | API — QA | Typecheck + build + tests backend | DONE | `npm run typecheck --workspace=@facturacion-simple/api` exit 0; `npm run build --workspace=@facturacion-simple/api` exit 0; `npm run lint` (todo el monorepo) exit 0; `npx vitest run apps/api/tests/recibos.service.test.ts` (19 tests, todos pasan) y `apps/api/tests/fiscal-gateway.test.ts` (20 tests, todos pasan). Corrida completa `npx vitest run apps/api/tests`: 149 passed, 6 failed — los 6 fallos (`entrega.service.test.ts`, `facturas.service.test.ts`, casos de "operational window") se confirmaron pre-existentes en `master` sin mis cambios (`git stash` + re-run identico), no relacionados a este trabajo. |
| RD5-016 | Deploy + QA real | Validar contra `fe-test` real y desplegar | BLOCKED | Bloqueado hasta que `facturacion-electronica` publique `/v1/recibos/*` en `fe-test` y la `fe_consumer_api_key` del facturador de prueba tenga `RECIBO_WRITE/READ/VOID`. Cuando se desbloquee: crear→emitir→anular contra fe-test real, luego `bash scripts/deploy.sh` a staging, y recien despues evaluar produccion con confirmacion explicita del usuario. |
| RD5-017 | UI (fuera de alcance v0.5) | Exponer `ANULADO`, XML, motivo de anulacion y `moneda` en `RecibosView` | TODO (v0.6) | Explicitamente fuera de esta iteracion, ver SPEC seccion 9. No se crea tarea de implementacion aqui, solo se deja registrado el pendiente. |

## Notas de producto

- El backend fiscal es la fuente de verdad del recibo desde su creacion (no un preview local, a diferencia de `facturas`) — ver SPEC seccion 3.1.
- Los recibos creados bajo v0.1-v0.4 (locales, sin firma) no se migran retroactivamente; siguen siendo legibles pero nunca tendran `xml_hash`/`pdf_hash`/`external_ref`.
- Ningun cambio de esta version se despliega a staging/produccion sin resolver antes RD5-016.

## Evidencia

- 2026-07-24: usuario pide (a) copiar de `FACTURACION_ELECTRONICA_MVP/.../facturacion-electronica-consumer-docs` lo relevante para esta app, (b) crear un SPEC de recibo de dinero/firma en base a eso, (c) actualizar la gestion/creacion de recibos (no la UI). Se verifica en la VPS que `/v1/recibos` no esta desplegado en fe-test/fe-prod (404 Cannot GET) antes de especificar nada, para no documentar una integracion sobre una capacidad inexistente en los entornos reales.
- 2026-07-24: se vendorizan los archivos relevantes del paquete fuente (README.md, GUIA_INTEGRACION_CONSUMIDORES_v0.2.md con la seccion 16 completa, openapi filtrado como `openapi.consumidor-v0.3.yaml`, postman, GUIA_MIGRACION_CONTRATO_CANONICO_v0.1.md y OPERACION_CONTRATO_CANONICO_v0.1.md actualizados) sin sobreescribir el `openapi.yaml` viejo (admin-inclusive, 88 paths incl. `/admin/*`) porque `SPEC_BACKOFFICE_ADMIN_v0.1.md`, `SPEC_AUTOGESTION_AVANZADA_SOPORTE_v0.1.md` y `SPEC_CONTRATO_CANONICO_FISCAL_v0.1.md`, entre otros, lo referencian directamente y el nuevo paquete filtrado (36 paths) no incluye esa superficie.
- 2026-07-24: se investiga el patron real de integracion `facturas`↔`fiscal-gateway` (modulo `apps/api/src/modules/fiscal-gateway/*`, usado hoy solo por `facturas`) para modelar `recibos` de forma consistente en vez de inventar un mecanismo nuevo. Diferencia de diseno relevante encontrada: `facturas` mantiene el borrador 100% local y solo llama al gateway al emitir; `recibos`, segun el contrato fiscal, ya es un recurso remoto desde el `POST /recibos` inicial — el diseno de esta version refleja esa diferencia (`recibos_dinero` pasa a ser cache/mirror, no fuente de verdad).
- 2026-07-24: SPEC, PLAN y TASKS redactados. Implementacion de RD5-003 en adelante pendiente de ejecutar en esta misma sesion.
- 2026-07-24: implementadas RD5-003..RD5-015 (backend completo: migracion, extension de `fiscal-gateway` con 9 metodos nuevos + mock/real, reescritura de `recibos.types/repository/service/routes.ts`, eliminacion de `recibos.pdf.ts`, `verificacion.routes.ts` hibrido, RD-010 actualizado, `spec/openapi.yaml` actualizado). Validaciones: `npm run typecheck`/`build`/`lint` OK; suite de tests de `recibos` (19) y `fiscal-gateway` (20) OK; corrida completa del backend sin regresiones nuevas (6 fallos preexistentes confirmados via `git stash`). No se toco ningun archivo de `apps/web-operacion` (fuera de alcance v0.5) ni se desplego nada a staging/produccion. `RD5-003` (ejecutar `npm run migrate` contra una DB real) y `RD5-016` (validacion e2e + deploy) quedan pendientes/bloqueados para cuando `facturacion-electronica` publique `/v1/recibos/*`.
