# TASKS Estabilizacion Operativa v0.1

## Alineacion

- `AGENTS.md`
- `docs/SPEC_PRODUCTO_MVP_v0.1.md`
- `docs/PLAN_IMPLEMENTACION_MVP_v0.1.md`
- `docs/OPERACION_PRODUCCION_MVP_v0.1.md`
- `docs/SPEC_ESTABILIZACION_OPERATIVA_v0.1.md`
- `docs/PLAN_ESTABILIZACION_OPERATIVA_v0.1.md`

## Matriz

| ID | Fase | Tarea | Estado | Criterio de aceptacion |
| --- | --- | --- | --- | --- |
| EST-001 | SDD | Crear SPEC/PLAN/TASKS de estabilizacion operativa | DONE | Existe esta cadena documental separada del MVP cerrado |
| EST-002 | QA FE | Registrar resultados reales de receptores SIFEN test | DONE | Quedan documentados CI `492019` aprobado y RUC `80000000-1` rechazado `1306` |
| EST-003 | Smoke | Mantener smoke operativo simple por facturador configurado | DONE | `npm run ops:onboarding-smoke` valida cliente, item, preview, emision FE y link publico sin crear facturador |
| EST-004 | Receptores | Crear catalogo local de receptores de prueba aprobables | DONE | Existe `docs/RECEPTORES_SIFEN_TEST_v0.1.md` con receptor aprobado `CI 492019` y receptor de rechazo controlado `RUC 80000000-1` |
| EST-005 | Diagnostico | Mejorar resumen de rechazo SIFEN en API/UI | DONE | Documento rechazado muestra codigo, mensaje, CDC/numero si existen y recomendacion operativa; la vista `Documentos` y resultado de emision usan `fiscal_status` resumido |
| EST-006 | Artefactos | Extender smoke operativo para validar KUDE/PDF y XML | PENDING_VALIDATION | `scripts/onboarding-smoke.cjs` valida documento publico, KUDE/PDF y XML con polling, pero FE devolvio 500 al pedir KUDE/PDF; queda pendiente de validacion externa de `fe-api.ventax.app` |
| EST-007 | NCE | Agregar smoke real de NCE total sobre factura aprobada | PENDING_VALIDATION | `scripts/onboarding-smoke.cjs` soporta `ONBOARDING_SMOKE_NCE=YES`; se corrigio payload NCE para enviar `timbrado.documentoNro` string y tests API pasan, pero no se reintenta FE por bloqueo externo |
| EST-008 | Backoffice | Documentar checklist manual de alta de facturador | DONE | Existe `docs/CHECKLIST_ALTA_FACTURADOR_MVP_v0.1.md` con pasos de emisor, timbrado, punto, actividad, operador, readiness y smoke |
| EST-009 | Catalogo UI | Agregar pantalla operativa de catalogo de productos/servicios | DONE | Operador lista, filtra, crea y edita items del catalogo del facturador |
| EST-010 | Documentos UI | Mejorar pantalla de documentos con consulta SIFEN y diagnostico fiscal | DONE | Operador ve codigo/mensaje SIFEN y puede consultar estado FE/SIFEN desde el detalle |
| EST-011 | UX Mobile | Reorganizar navegacion operativa con menu hamburguesa | DONE | Operador navega Inicio, Nueva factura, Catalogo y Documentos desde menu mobile |
| EST-012 | Entorno Local | Configurar `admin/admin` como operador del facturador test | DONE | Login admin muestra readiness listo y permite emitir desde UI contra FE test |

## Evidencia

- 2026-05-19: `EST-001` creado para separar estabilizacion operativa del MVP ya cerrado.
- 2026-05-19: `EST-002` documenta resultados reales: `0002106` aprobado para CI `492019`; `0002104` y `0002105` rechazados para RUC generico `80000000-1` con codigo SIFEN test `1306`.
- 2026-05-19: `EST-003` ya cuenta con `scripts/onboarding-smoke.cjs` y ejecucion exitosa previa: `estado EMITIDA`, numero `0002106`, CDC `01801369681001001000210622026051918996595030`.
- 2026-05-19: `EST-009` implementa vista `Catalogo` en web-operacion con listado, filtros, alta y edicion sobre `/catalogo/items`.
- 2026-05-19: `EST-010` mejora vista `Documentos` y resultado de emision: muestra resumen SIFEN desde `fiscal_status` y expone la accion `Consultar SIFEN` sobre `/facturas/{id}/refresh-status`.
- 2026-05-19: Validacion `EST-009`/`EST-010`: `npm run typecheck --workspace @facturacion-simple/web-operacion`, `npm run lint --workspace @facturacion-simple/web-operacion`, `npm run build --workspace @facturacion-simple/web-operacion`, `npm run qa:no-secrets`, `bash scripts/deploy.sh`, `curl -sS http://127.0.0.1:8092/healthz`, `curl -sS http://127.0.0.1:8092/api/v1/health`, `docker compose ps` y Playwright mobile contra `http://127.0.0.1:8092/app/`.
- 2026-05-19: Regla de validacion actualizada: las validaciones HTTP, smoke y visuales de comportamiento real deben ejecutarse contra contenedores redeployados con `bash scripts/deploy.sh`.
- 2026-05-19: `EST-011` implementa navegacion mobile con menu hamburguesa para Inicio, Nueva factura, Catalogo, Documentos y Salir.
- 2026-05-19: `EST-012` configura localmente `admin/admin` con contexto operativo activo del facturador test `80136968-1`, establecimiento `001`, punto `001`, actividad `82110`, perfil local `SERV`.
- 2026-05-19: Validacion `EST-011`/`EST-012`: `npm run typecheck --workspace @facturacion-simple/web-operacion`, `npm run lint --workspace @facturacion-simple/web-operacion`, `npm run build --workspace @facturacion-simple/web-operacion`, `npm run qa:no-secrets`, `bash scripts/deploy.sh`, healthchecks `frontend`/`api`, Playwright mobile contra contenedores con `admin/admin`, readiness `ready: true`, y `SMOKE_USERNAME=admin SMOKE_PASSWORD=admin npm run ops:onboarding-smoke`.
- 2026-05-19: Smoke operativo real con `admin/admin` emitio factura FE test `0002107`, estado `EMITIDA`, CDC `01801369681001001000210722026051912601909677`, `smokeId` `onboarding-20260519032022114`.
- 2026-05-19: `EST-004` cerrado con `docs/RECEPTORES_SIFEN_TEST_v0.1.md`, catalogando `CI 492019` como receptor aprobable y `RUC 80000000-1` como rechazo controlado `1306`.
- 2026-05-19: `EST-005` cerrado con diagnostico SIFEN ya expuesto en API/UI desde `fiscal_status`: codigo, mensaje, numero/CDC cuando existen y recomendacion operativa para rechazos como `1306`.
- 2026-05-19: `EST-006` queda `PENDING_VALIDATION`: el smoke fue extendido para leer `public_url`, validar documento publico y fallar si KUDE/PDF o XML no responden. Validacion contra contenedores llego a FE, pero KUDE/PDF devolvio `502` local por `gateway_code: UPSTREAM_ERROR` y `details.status: 500` desde FE.
- 2026-05-19: `EST-007` queda `PENDING_VALIDATION`: el smoke fue extendido con `ONBOARDING_SMOKE_NCE=YES` y se corrigio el payload NCE para enviar `timbrado.documentoNro` string aun con `FE_SERVICE_NUMBERING=true`. Validacion local: `npm run test --workspace @facturacion-simple/api`, `npm run build --workspace @facturacion-simple/api`, `npm run qa:no-secrets` y `bash scripts/deploy.sh`. No se reintenta FE por instruccion operativa ante error externo.
- 2026-05-19: `EST-008` cerrado con `docs/CHECKLIST_ALTA_FACTURADOR_MVP_v0.1.md`.
- 2026-05-19: agregado log estructurado `fiscal_artifact_fetch_failed` para fallos de KUDE/PDF/XML con `requestId`, hora, endpoint, artefacto, CDC, numero fiscal, estado del documento y detalle del gateway. Validacion: `npm run test --workspace @facturacion-simple/api` y `npm run build --workspace @facturacion-simple/api`.
- 2026-05-20: diagnostico de produccion solo lectura en VPS `cobrapy-mvp-01`: `facturacion-simple` y `facturacion-electronica` estaban healthy; `FE_API_BASE_URL=http://host.docker.internal:9988/fcws`; logs FE mostraron respuesta `409` idempotente con documento existente `APPROVED`, CDC y numero fiscal, que el SaaS trataba como `UPSTREAM_ERROR` temporal. Se ajusto `RealFiscalGateway` para mapear `409` solo cuando `idempotent=true` y existen `document_id` o `cdc`, conservando como error los conflictos no idempotentes. Validacion ejecutada: `npm run test --workspace @facturacion-simple/api -- fiscal-gateway`, `npm run test`, `npm run typecheck`, `npm run lint`, `npm run build` y `npm run qa:no-secrets`.
- 2026-05-20: corregida propagacion de email de clientes guardados hacia FE. Diagnostico: el FiscalGateway ya enviaba `receptor.email` si `input.cliente.email` llegaba en el body, pero la emision usaba literalmente el snapshot del formulario; si el cliente tenia email en agenda y el request venia sin email, FE recibia receptor sin email y la entrega mostraba `Cliente sin email registrado`. Se agrego resolucion backend por `cliente_id` contra la agenda del facturador para completar `email`, `telefono` y `direccion` antes de encolar la emision/fiscal request, sin exponer secretos. Validacion enfocada: `npm run test --workspace @facturacion-simple/api -- facturas.service clientes.service fiscal-gateway` y `npm run typecheck --workspace @facturacion-simple/api`.
- 2026-05-22: ampliado `EST-008` con `docs/GUIA_PRODUCCION_ALTA_CLIENTE_FINAL_v0.1.md`, runbook de alta productiva desde servidor para tenant, facturador, contexto fiscal local, bootstrap de primer admin, formato normalizado de ficha FE, multiples puntos de expedicion y operadores via endpoints backoffice. Se habilito username con formato email para operadores, por ejemplo `emiliomatasc@fpuna.edu.py`. Validacion: `npm run test --workspace @facturacion-simple/api -- backoffice.service`, `npm run typecheck --workspace @facturacion-simple/api` y `npm run qa:no-secrets`.
- 2026-05-22: agregada guia `docs/GUIA_DEPLOY_STAGING_PRODUCCION_v0.1.md` y soporte operativo para dos instancias en el mismo VPS con `APP_ENV_FILE`, plantillas `.env.staging.example`/`.env.production.example`, Nginx multiambiente y separacion de FE test/prod (`9988`/`9989`). Ajuste posterior: `scripts/deploy.sh` exporta `APP_ENV_FILE` para que `docker-compose.yml` lea el env correcto; Postgres queda parametrizado por `POSTGRES_DB`, `POSTGRES_USER`, `POSTGRES_PASSWORD` y `DATABASE_URL`; produccion usa credencial Postgres propia sobre volumen nuevo sin copiar la base actual. Validacion no destructiva: `bash -n scripts/deploy.sh scripts/backup.sh scripts/restore-backup.sh scripts/verify-restore.sh`, `APP_ENV_FILE=.env.production.example docker compose --env-file .env.production.example -f docker-compose.yml config --services`, `APP_ENV_FILE=.env.staging.example docker compose --env-file .env.staging.example -f docker-compose.yml config --services` y `npm run qa:no-secrets`.
- 2026-05-22: estabilizada conectividad real en VPS `cobrapy-mvp-01`: los contenedores SaaS `api` se conectan a redes Docker externas `fe-test_default` y `fe-prod_default`; staging usa `FE_API_BASE_URL=http://fe-test-api-1:8080/fcws` y produccion `FE_API_BASE_URL=http://fe-prod-api-1:8080/fcws`. Se evita `host.docker.internal` porque FE publica `9988/9989` solo en loopback del host, y se evita DNS publico porque Cloudflare bloquea requests originados desde el VPS. `DATABASE_URL` usa nombres explicitos de contenedor Postgres SaaS para evitar colision del hostname `postgres` con las redes FE. Validacion en VPS: health de frontend/API staging y produccion OK, conectividad FE test/prod desde cada API OK, conexion `pg` desde ambos API OK, login staging con `esaldivar1811` OK y `GET /me/readiness` devuelve `ready:true`.
- 2026-05-22: alta productiva de facturador `5057016-1` en `ventax-facturacion-simple-prod`: tenant `emilio-saldivar`, establecimiento `001`, puntos `001`/`002`, perfiles `A45203-E001-P001-FE-PTO` y `A96099-E001-P002-FE-PTO`, timbrado `18861677`, numeracion local de referencia `0000001`, alias operativos `TALLER ROBERT` y `1811 BRANDING Y SOFTWARE`. Se crearon/actualizaron 5 operadores solicitados y se valido login real + `ready:true` contra `http://127.0.0.1:8192/api/v1`: `esaldivar1811` y `csaldivar1811` en punto `002`; `esaldivartaller`, `fvillalbataller` y `csaldivartaller` en punto `001`. Health final de FE prod desde API y health publico de `https://factura.ventax.app/api/v1/health` OK. No se ejecuto smoke NCE porque FE informa NCE no habilitada.
- 2026-05-22: aclarada e implementada regla de entrega publica para `EST-006`: KUDE/PDF y XML deben estar disponibles desde `/public/d/{token}` siempre que exista CDC y artefacto local en `facturacion-electronica`, sin exigir estado `EMITIDA` ni aprobacion SIFEN. Se actualizan SPEC, PLAN, OpenAPI, test unitario de entrega para cubrir documento `RECHAZADA` con CDC y UI operativa para habilitar botones segun `delivery.artifacts.*.available`, no por estado. Validacion: `npm run test --workspace @facturacion-simple/api -- entrega.service.test.ts`, `npm run typecheck --workspace @facturacion-simple/api`, `npm run typecheck --workspace @facturacion-simple/web-operacion`, `npm run build --workspace @facturacion-simple/web-operacion` y `npm run qa:no-secrets`. Playwright mock mobile contra preview local no cerro por expectativa desactualizada del script sobre texto `Puede emitir`.
- 2026-05-23: redeploy en VPS `cobrapy-mvp-01` de staging `ventax-facturacion-simple` y produccion `ventax-facturacion-simple-prod` con `APP_ENV_FILE=.env.staging` y `.env.production`, backups previos `facturacion_simple-20260523-031428.dump` para ambos ambientes y health local OK en `8092`/`8192`. Alta productiva de facturador `4732402-3`: tenant `despensa-graciela`, nombre fantasia `DESPENSA GRACIELA`, establecimiento `001`, punto `001`, actividad `47112`, perfil `A47112-E001-P001-FE-COMP`, timbrado `18868794`, inicio `2026-05-21`, numeracion local de referencia `0000001`, modo fiscal `BATCH` y `batch_enabled=true`. Se crearon/actualizaron operadores `garevalosdespensa` y `aarevalodespensa` con configuracion operativa activa; login real y `GET /me/readiness` devuelven `ready:true` para ambos contra `http://127.0.0.1:8192/api/v1`. No se ejecuto smoke fiscal productivo porque implicaria emitir un comprobante real sin autorizacion explicita; NCE no aplica porque la ficha FE informa `nota_credito_electronica_habilitada=false`. Validacion publica por Nginx local con Host header OK para `staging-factura.ventax.app` y `factura.ventax.app`; requests publicos desde el propio VPS via Cloudflare quedan bloqueados por regla externa, consistente con la nota operativa previa.
- 2026-05-23: corregido contrato SaaS -> FE para mantener `BATCH` como envio fiscal por defecto sin `sendNow:true`; FE prod rechaza `sendNow:true` cuando `envio.mode=BATCH`. `RealFiscalGateway` ahora solo incluye `sendNow:true` en modo `SYNC` explicito y omite ese flag en `BATCH`, tambien para NCE. Validacion local: `npm run test --workspace @facturacion-simple/api -- fiscal-gateway` y `npm run typecheck --workspace @facturacion-simple/api`.
- 2026-05-23: alineado branding de la entrega publica de comprobantes: `renderPublicDocumentHtml` deja de mostrar la marca textual con inicial `V` y usa el asset oficial `/app/brand/VENTAX-PRINCIPAL.svg`, consistente con la app operativa. Validacion local: `npm run test --workspace @facturacion-simple/api -- entrega.service` y `npm run typecheck --workspace @facturacion-simple/api`.

## Pendiente De Validacion Externa

Quedan fuera de DONE hasta que FE responda correctamente:

- `EST-006`: KUDE/PDF y XML para factura aprobada.
- `EST-007`: NCE total real con `ONBOARDING_SMOKE_NCE=YES`.
